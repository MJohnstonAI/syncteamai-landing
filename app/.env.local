# Sending account (use your SMTP provider; examples below)
SMTP_HOST=smtp.gmail.com
SMTP_PORT=465
SMTP_SECURE=true
SMTP_USER=your_gmail_username@gmail.com
SMTP_PASS=your_gmail_app_password  # use an App Password if Gmail

# Who receives notifications (your inbox)
ADMIN_EMAIL=syncteamai@gmail.com

# From header used in emails you send out
FROM_EMAIL="SyncTeamAI <no-reply@syncteamai.com>"
